export type CabType = { id: string; name: string; price: string; image: string };

export type BookingType = {
  id: string;
  source: string;
  destination: string;
  price: string;
  email: string;
  cabImage: string;
  startTime: string;
  endTime: string;
};
